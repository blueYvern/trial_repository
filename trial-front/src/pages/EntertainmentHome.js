import React from "react";
import TabHeader from "../templates/TabHeader";

function EntertainmentHome({tabTitle}) {
    return (
        <TabHeader tabTitle={tabTitle}/>
    );
}

export default EntertainmentHome;